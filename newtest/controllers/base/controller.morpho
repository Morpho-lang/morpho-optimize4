// Test controller base class 
import optimize4
import "../../examples/constrainedquadratic.morpho"

var adapt = ConstrainedQuadraticExample().build() 

var con = OptimizationController(adapt)

print con.value() 
// expect: 0

print con.constraintValue()
// expect: [ -1, 1 ]
