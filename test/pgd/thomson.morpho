
import "../examples/thomson.morpho"

var example = Thomson()
var adapt = example.build()

var control = ProjectedGradientDescentController(adapt, steplimit=0.005)
control.optimize(10000) 

example.visualize() 
