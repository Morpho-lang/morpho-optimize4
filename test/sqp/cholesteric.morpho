
import optimize4
import "../examples/cholesteric.morpho" for CholestericExample

var example = CholestericExample()
var adapt = example.build()

var control = SQPController(adapt)
control.optimize(1000) 

example.visualize()
