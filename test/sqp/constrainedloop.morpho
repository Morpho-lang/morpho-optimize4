
import optimize4
import plot
import "../examples/constrainedloop.morpho"

var example = ConstrainedLoop()
var adapt = example.build()

var control = SQPController(adapt)
control.optimize(100) 

Show(example.visualize()) 

// Fails on this problem
